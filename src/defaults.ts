/** Matches today's behavior exactly - a config lacking these fields must produce identical output. */
export const DEFAULT_TICKET_PATTERN = "([A-Za-z]{2,4}-\\d+)";
export const DEFAULT_BASE_BRANCHES = ["main", "master", "develop"];
